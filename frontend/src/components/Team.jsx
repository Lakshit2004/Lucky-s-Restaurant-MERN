import React from "react";
import { data } from "../restApi.json";
const Team = () => {
  return (
    <section className="team" id="team">
      <div className="container">
        <div className="heading_section">
          <h1 className="heading">OUR TEAM</h1>
            <p>
              Our strength lies in our diversity. Led by chefs trained in both
              timeless Continental techniques and the rich traditions of Indian
              cuisine, our team brings a world of experience to your plate.
              Every dish is a collaboration of skill, creativity, and a shared
              passion for exceptional food.
            </p>
        </div>
        <div className="team_container">
          {data[0].team.map((element) => {
            return (
              <div className="card" key={element.id}>
                <img src={element.image} alt={element.name} />
                <h3>{element.name}</h3>
                <p>{element.designation}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
};

export default Team;
